ARCHITECTURE=aarch64
QEMU_MACHINE=phytium-pi
RELEASE_ASSET_PREFIX=aarch64-phytium-pi
DEFAULT_BOOT_MODE=firmware
SUPPORTED_BOOT_MODES=(firmware linux)
FIRMWARE_IMAGES=(sdcard.img)
LINUX_IMAGES=(Image.gz phytiumpi_firefly.dtb rootfs.cpio.gz)
REQUIRED_IMAGES=("${FIRMWARE_IMAGES[@]}" "${LINUX_IMAGES[@]}")
